<div class="max-w-3xl mx-auto px-4">
  <div class="flex items-center justify-between">
    <h1 class="flex-1 font-bold text-3xl">Blazerand_jacket</h1>

    <div class="flex-wrap lg:flex items-center lg:space-x-3">
      <%= link_to blazerand_jackets_path, class: button_class(theme: "transparent") do %>
      All Blazerand_jackets
      <% end %>
      <%= link_to edit_blazerand_jacket_path(@blazerand_jacket), class: button_class(theme:"primary") do %>
      Edit
      <% end %>
    </div>
  </div>

  <ul class="text-slate-700">
      <li>
        <p class="font-semibold">Fullname:</p>
        <p><%= @blazerand_jacket.Fullname %></p>
      </li>
      <li>
        <p class="font-semibold">Description:</p>
        <p><%= @blazerand_jacket.Description %></p>
      </li>
      <li>
        <p class="font-semibold">Quantity:</p>
        <p><%= @blazerand_jacket.Quantity %></p>
      </li>
      <li>
        <p class="font-semibold">Price:</p>
        <p><%= @blazerand_jacket.Price %></p>
      </li>
      <li>
        <p class="font-semibold">Url:</p>
        <p><%= @blazerand_jacket.Url %></p>
      </li>
  </ul>
</div>
